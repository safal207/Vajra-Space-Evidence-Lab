# M87 `eht-imaging` OCI Environment

## Verified identity

- OCI reference: `ghcr.io/safal207/vajra-m87-ehtim@sha256:a295853ec4de8ac44523c342e008b0d02b28a3f11aa213e0b777013829db2af3`
- Platform: `linux/amd64`
- Base image: `mambaorg/micromamba:1-bookworm-slim@sha256:e3797091302382ea841498bc93a7b0a50f7c1448333d5e946d2d1608d0c5f43d`
- Exact Conda lock: `containers/m87-ehtim/explicit-linux-64.txt`
- Lock SHA-256: `e4f061d38ccc642dd55cf4e4d100319749e632b001c1b0c5c0ceb5bf10061448`

## Upstream source

- Repository: `https://github.com/achael/eht-imaging.git`
- Commit: `22ae35f307921a4d423aa69f6aed6e93a74ecbc0`
- Tree SHA-256: `efea21644ef3dc837b878431bf1081c4e7e1570084a690e57cfa2ec0714fe5ef`
- Declared package version: `1.1.0`

## Audited compatibility patch

Upstream `eht-imaging 1.1.0` contains two Python 2 syntax forms in `ehtim/parloop.py` that prevent Python 3 import. Vajra applies one auditable patch that only changes syntax:

- `print 'pool terminated'` → `print('pool terminated')`
- `except Exception, e:` → `except Exception as e:`

Patch path: `containers/m87-ehtim/patches/ehtim-1.1.0-python3-parloop.patch`

Patch SHA-256: `7b432916770f1e45108329aff59b4200e148b864e564ab669a5a841402e7d167`

The patch does not alter imaging parameters, numerical algorithms, or calibration logic. Two byte-identical reconstructed FITS outputs are evidence that the patched environment is internally repeatable, but an unpatched independent reference execution is still needed to confirm output equivalence.

## Runtime dependencies discovered through execution

The historical package metadata did not declare all dependencies used by the released M87 pipeline. Fail-closed executions exposed and then pinned:

- `IPython 7.16` — required by the package import path;
- `pandas 0.25.3` — required by `avg_coherent()` through `ehtim.statistics.dataframes`;
- `pytz 2023.3.post1` — resolved dependency of the pinned pandas package.

These packages are now part of the exact Conda lock rather than installed dynamically.

## Environment verification

GitHub Actions run `30208007320` completed successfully and verified:

1. immutable base-image resolution;
2. deterministic Conda environment solving;
3. exact lock generation;
4. upstream source-tree and patch hashes;
5. GHCR image build and push with SBOM and provenance;
6. pull of the published image by digest;
7. successful import of `ehtim 1.1.0`;
8. successful start of the official M87 pipeline help path.

Environment artifact digest: `sha256:6eb4ab747ced2c25e96d15509cb39942cf987e0f7f7f74ad08b43cce8756dc18`

## Scientific bootstrap reproduction

GitHub Actions run `30208439097` executed the official April 11 low/high-band reconstruction twice with:

- container network disabled;
- read-only root filesystem;
- all Linux capabilities dropped;
- `no-new-privileges` enabled;
- fixed thread counts and `PYTHONHASHSEED=0`;
- exact input, pipeline, and OCI hashes.

Both runs generated byte-identical FITS files:

- FITS SHA-256: `70db37ed8661c6354976f071d4911f77f106fc5f99bcdc0d66a8d2a2ffff16ad`
- canonical-pixel SHA-256: `432f97dbc5ba73f6dfb54be6a948911f8c3690c2e28f7b690979038c369ac6c2`
- artifact digest: `sha256:426bf04167f25e4974186bd045815ed96aaad1efd4145ed294955aa416ad4f80`

Bootstrap diagnostics produced an approximate radial peak diameter of `38.5 μas` and radial FWHM of `16 μas`. These values are diagnostic outputs from Vajra's initial analyzer; they are not yet the independently reviewed EHT ring-measurement contract.

## Remaining scientific work

- reproduce the same FITS output through independent execution infrastructure or an independent operator;
- validate image-feature extraction against the official EHT image-ensemble methodology;
- test cross-platform and alternate numerical-library stability;
- represent independent confirmations as machine-readable evidence objects;
- publish the complete Evidence Card without collapsing reconstruction into physical inference.
